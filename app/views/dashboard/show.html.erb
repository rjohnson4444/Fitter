  <div class="ui grid">
    <div class="row">
      <div class="five wide column">
        <p><%= render partial: 'partials/user_info', locals: { user: @user } %></p>
        <p><%= render partial: 'partials/post_tweet', locals: { user: @user } %></p>
      </div>
      <div class="eight wide column">
        <p><%= render partial: 'partials/tweet_feed', locals: { tweet_info: @tweet_info } %></p>
      </div>
      <div class="three wide column">
        <img>
      </div>
    </div>
  </div>
